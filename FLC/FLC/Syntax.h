#pragma once

namespace flc
{
    namespace syntax
    {
        class Syntax
        {
        public:
            Syntax();
            ~Syntax();

            virtual string toString();
        };
    }
}